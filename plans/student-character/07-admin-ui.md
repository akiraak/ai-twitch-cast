# Step 7: 管理画面UI

## ステータス: 未着手

## ゴール

教師モード管理画面に生徒キャラの設定UI（名前・TTS音声・VRM・有効/無効）と、スクリプトプレビューの話者色分けを追加する。

## 変更対象

| ファイル | 変更内容 |
|---------|---------|
| `scripts/routes/teacher.py` | 生徒設定API |
| `static/js/admin/teacher.js` | 設定UI・プレビュー色分け |

## 前提

- Step 4（DB設定キー）完了済み

## 実装

### 7-1. API

- `GET /api/settings/student` — 生徒キャラ設定取得
- `PUT /api/settings/student` — 生徒キャラ設定更新

### 7-2. 管理画面

- 有効/無効チェックボックス
- 名前・TTS音声セレクト・TTSスタイルテキストエリア
- VRMモデルセレクト（`/api/files/avatar/list` から動的生成）

### 7-3. スクリプトプレビュー色分け

- 🎓 ちょビ → 青系
- 🎒 まなび → ピンク系

## 完了条件

- [ ] 管理画面で生徒設定を変更できる
- [ ] スクリプトプレビューで話者が色分け表示される
